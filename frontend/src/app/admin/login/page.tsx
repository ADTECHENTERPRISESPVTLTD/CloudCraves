"use client";
import { useState } from "react";
import { useRouter } from "next/navigation";
import { authService } from "@/lib/services/auth.service";
export default function AdminLogin(){const router=useRouter();const [email,setEmail]=useState("admin@cloudkitchen.local"),[password,setPassword]=useState("admin123"),[error,setError]=useState("");return <div className="flex min-h-screen items-center justify-center bg-[#6b4f3a] p-4"><form onSubmit={async e=>{e.preventDefault();try{await authService.login(email,password);router.push("/admin")}catch(err){setError(err instanceof Error?err.message:"Invalid login")}}} className="card-kitchen w-full max-w-md p-7"><p className="text-sm font-black text-[#e4572e]">LOCALBITE</p><h1 className="mt-2 text-3xl font-black text-[#6b4f3a]">Admin sign in</h1><p className="mt-2 text-sm text-[#6d625a]">Kitchen owner/staff portal.</p><label className="mt-6 block text-sm font-bold">Email<input className="input-kitchen mt-1" value={email} onChange={e=>setEmail(e.target.value)}/></label><label className="mt-4 block text-sm font-bold">Password<input type="password" className="input-kitchen mt-1" value={password} onChange={e=>setPassword(e.target.value)}/></label>{error&&<p className="mt-3 text-sm text-[#b33b21]">{error}</p>}<label className="mt-4 flex items-center gap-2 text-sm"><input type="checkbox"/> Remember session</label><button className="btn-primary mt-6 w-full">Sign in</button></form></div>}
