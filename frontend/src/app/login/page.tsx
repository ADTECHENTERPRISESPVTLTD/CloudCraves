"use client";
import Link from "next/link";
import { useState } from "react";
import { useRouter } from "next/navigation";
import { authService } from "@/lib/services/auth.service";
export default function LoginPage(){const [email,setEmail]=useState("customer@example.com"),[password,setPassword]=useState("customer123"),[loading,setLoading]=useState(false),[error,setError]=useState("");const router=useRouter();return <div className="flex min-h-screen items-center justify-center bg-[#fff1e8] p-4"><form onSubmit={async e=>{e.preventDefault();setLoading(true);setError("");try{await authService.login(email,password);router.push("/")}catch(err){setError(err instanceof Error?err.message:"Login failed")}finally{setLoading(false)}}} className="card-kitchen w-full max-w-md p-7"><h1 className="text-3xl font-black text-[#6b4f3a]">Welcome back</h1><p className="mt-2 text-sm text-[#6d625a]">Customer login for the prototype.</p><label className="mt-6 block text-sm font-bold">Email<input className="input-kitchen mt-1" value={email} onChange={e=>setEmail(e.target.value)}/></label><label className="mt-4 block text-sm font-bold">Password<input type="password" className="input-kitchen mt-1" value={password} onChange={e=>setPassword(e.target.value)}/></label>{error&&<p className="mt-3 text-sm text-[#b33b21]">{error}</p>}<button disabled={loading} className="btn-primary mt-6 w-full">{loading?"Signing in…":"Sign in"}</button><p className="mt-5 text-center text-sm">New here? <Link href="/register" className="font-bold text-[#e4572e]">Create account</Link></p></form></div>}
