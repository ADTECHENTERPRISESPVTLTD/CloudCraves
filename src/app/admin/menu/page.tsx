"use client";
import { useState } from "react";
import AdminShell from "@/components/admin/AdminShell";
import { mockMenus } from "@/data/mockMenus";
export default function AdminMenu(){const [items,setItems]=useState(mockMenus.filter(x=>x.restaurantId==="r1"));const [show,setShow]=useState(false);return <AdminShell><div className="flex flex-col justify-between gap-3 sm:flex-row"><div><h2 className="text-3xl font-black text-[#6b4f3a]">Menu management</h2><p className="mt-1 text-sm text-[#6d625a]">Categories, items, prices and availability.</p></div><button onClick={()=>setShow(!show)} className="btn-primary">+ Add item</button></div>{show&&<div className="card-kitchen mt-5 p-5"><div className="grid gap-3 sm:grid-cols-2"><input className="input-kitchen" placeholder="Item name"/><input className="input-kitchen" placeholder="Price"/><input className="input-kitchen sm:col-span-2" placeholder="Description"/></div><button className="btn-primary mt-4" onClick={()=>setShow(false)}>Save item</button></div>}<div className="mt-5 grid gap-4 md:grid-cols-2">{items.map(i=><div key={i.id} className="card-kitchen flex gap-4 p-4"><img src={i.image} alt={i.name} className="h-24 w-24 rounded-xl object-cover"/><div className="flex-1"><div className="flex justify-between gap-2"><h3 className="font-extrabold">{i.name}</h3><b>₹{i.price}</b></div><p className="mt-1 text-sm text-[#6d625a]">{i.description}</p><button className={`mt-3 rounded-full px-3 py-1 text-xs font-bold ${i.isAvailable?"badge-open":"badge-closed"}`} onClick={()=>setItems(items.map(x=>x.id===i.id?{...x,isAvailable:!x.isAvailable}:x))}>{i.isAvailable?"Available":"Unavailable"}</button></div></div>)}</div></AdminShell>}
